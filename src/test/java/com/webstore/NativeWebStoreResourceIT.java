package com.webstore;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeWebStoreResourceIT extends WebStoreResourceTest {

    // Execute the same tests but in native mode.
}